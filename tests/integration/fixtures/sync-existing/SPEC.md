# Sync Test

This fixture tests the sync command with an existing status.json file.

## Deliverables

### DL-001: Existing File

Create a file named `existing.txt` with the content "Hello".

#### Acceptance Criteria

- File `existing.txt` exists in project root
- File contains "Hello"

### DL-002: New File

Create a file named `new.txt` with the content "World".

#### Acceptance Criteria

- File `new.txt` exists in project root
- File contains "World"
