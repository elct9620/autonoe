services:
  cli:
    build:
      context: .
      dockerfile: apps/cli/Dockerfile
      target: node
    volumes:
      - ./tmp:/workspace
    working_dir: /workspace
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
